# Checking if it is game over might have bugs -> finished
# Show a message dialog when there is no steps to move for each player for one second
# Change if (gameProp.isProcessingJob) to isProcessingJob() for every viewmodel