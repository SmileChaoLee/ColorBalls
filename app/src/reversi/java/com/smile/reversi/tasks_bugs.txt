# Checking if it is game over might have bugs -> finished
# Change ShowMessageOnScreen() to a dialog --> finisshed
# Show a message dialog when there is no steps
# to move for each player for one second --> finished
# Change if (gameProp.isProcessingJob) to isProcessingJob() for every viewmodel